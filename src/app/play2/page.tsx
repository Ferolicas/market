import Home from "../page";

/** The same shell as the home page; `GameShell` mounts the plain-three
 * client (`src/client`) when the path starts with /play2. */
export default function PlayTwo() {
  return <Home />;
}
